# 问枝知乎账号授权接入

## 当前状态

已实现可选连接入口、服务端回调、一次性 state 校验、短期 HttpOnly 会话和退出连接。没有凭据时显示未开放，不影响游客模式。仅记录授权状态，不读取用户创作、收藏或关注，不保存用户 OAuth token，不绑定或同步浏览器探索记录。

尚未完成真实授权联调，也未发布这轮授权代码。线上目前只有内容检索 Access Secret，没有 OAuth 应用凭据。

## 申请材料

根据项目保存的知乎接入资料，申请收件人为 product-platform@zhihu.com；实际申请要求以平台回复为准。本文件是邮件草稿，尚未发送。

主题：问枝申请接入知乎 OAuth 服务

您好，我们希望为“问枝”网页版申请知乎 OAuth 应用。

应用名称：问枝

应用地址：https://wenzhi.autumn427.xyz/

应用简介：通过不同选择的模拟对照，帮助用户梳理行动取舍，并结合可追溯的知乎公开内容制定现实小实验。本阶段仅接入可选账号授权，不读取创作、关注或收藏。

申请回调地址：https://wenzhi.autumn427.xyz/api/auth/zhihu/callback

申请人姓名、手机号：由申请人填写。

请提供专用 app_id、app_key，并确认授权请求中的 state 是否会原样回传；若不支持，请提供可验证请求关联性的官方替代协议。另请确认授权成功回调字段与 token 响应格式。

## 配置与上线

- `ZHIHU_OAUTH_APP_ID`：应用 ID。
- `ZHIHU_OAUTH_APP_KEY`：只放 Cloudflare Secret，不写源码、前端或聊天。
- `ZHIHU_OAUTH_REDIRECT_URI`：必须与上方公网回调和平台登记值完全一致。
- 先执行新增迁移 `0005_create_zhihu_oauth.sql`，再发布代码。
- 本地测试使用模拟接口；正式流程必须在登记的 HTTPS 域名由用户亲自授权。
- 不接受缺少 state 的回调。已有参考资料记录过知乎不返 state，因此不能仅凭代码测试通过就宣称真实登录可用。
- 退出仅删除本站会话，不宣称撤销知乎侧授权。

## 本地验收

`node scripts/test-zhihu-oauth.mjs` 使用虚构凭据，检查未配置、跨站请求、缺少 state/浏览器 cookie、成功交换、会话轮换、回调重复使用及退出。不调用真实知乎账户。
