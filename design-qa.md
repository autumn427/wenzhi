# 分阶段纸雕背景与刘看山验证

final result: passed

## 视觉依据
- 保留既有 galgame 的章节牌、左侧对白/选择、右侧方角白底蓝边知乎卡片，本次增加阶段背景与角色。
- 原始风格：public/assets/galgame/parttime-blue.png、internship-amber.png、market-green.png。
- 新目标：public/assets/galgame/day{90,150}-{blue,amber,green}.png，每张 1672 × 941。
- 用户修订：阶段颜色太相似。90 天保持沉静的蓝、琥珀、鼠尾草绿，150 天改为冰川青蓝、杏金奶油、嫩黄绿。旧版留在 .runtime/galgame-qa/superseded/。
- 桌面截图：.runtime/galgame-qa/day90-{a,b,c}.png 与 day150-{a,b,c}.png。
- 同一比较输入：.runtime/galgame-qa/stages-comparison.png，将每个宇宙 90 天素材/页面与 150 天素材/页面并列查看；另有 stages-overview.png 概览。
- 桌面 viewport 设置 1440 × 900，截图返回 1309 × 818；比较板等比例缩放，没有将截图像素误当 CSS 像素。背景以 cover 适应槽位，交互 UI 为独立图层。
- 手机 viewport 设置 390 × 844：.runtime/galgame-qa/stages-mobile.png；另检查选择、纵向滚动与知乎卡片。

## 比较结果
- 字体/层级：保留既有正文、章节牌、知乎样式，角色不占用文字宽度。手机文字自然换行，无角色遮挡。
- 间距/布局：角色在左栏独立占位，在对白或选择上方；减少原有顶部空白容纳角色。桌面知乎区独立，手机移至故事下方。
- 色彩：150 天与 90 天的色温、明度明显区分；对白纸笺维持可读性，知乎蓝白保持不变。
- 图片：六张真图片保留纸层切面及阴影。刘看山使用用户已有透明 GIF，无白色矩形底；没有重新生成角色。
- 内容：新场景为学习、讨论、廊道、阅读角、庭院等通用空间，无职业标签、文字或预设成功结局。剧情和知乎内容仍走原数据链路。
- 交互：通过真实试玩按钮，三个宇宙均从 30 推进到 90、150 天。试选保留当前背景，确认后切换下一阶段；切换宇宙保留独立进度。
- 动画：暂停后 img 切到 PNG，播放恢复 GIF；提供系统减少动态效果的 picture/source 静帧。未自动切换操作系统设置。
- 浏览器 error 日志为空。

## 修订历史
1. 首轮六张背景和角色接入后，用户反馈阶段颜色相似，按 P2 处理。
2. 内置 imagegen 限定编辑三张 150 天图的配色，保留构图；重新打开各宇宙、捕获截图并与 90 天并列比较。
3. 当前无未解决的 P0/P1/P2，手机和桌面均可继续阅读和选择。

## 验证及限制
- TypeScript 检查通过。
- scripts/test-fork-comparison.mjs 的 24 条预设路径通过。
- 生产构建通过；既有大 chunk 提示不影响本次资源加载。
- 自定义故事共用 galSceneAssets(code, day)，不依赖标题或职业关键词。本次未额外发起付费自定义剧情生成。
- 未部署。

## 树状路径地图更新（2026-09-14）

final result: passed

- 本次源图：用户提供的 codex-clipboard-673e747c-ffac-455f-b929-2c8cb8244223.png（旧详情布局）与新生成 public/assets/galgame/journey-map-paper.png（1672 × 941）。用户要求将旧重复区域替换为图形地图，因此不以复制旧布局为目标。
- 合并比较输入：.runtime/galgame-qa/tree-comparison.png；页面证据 tree-branch.png、tree-ticket.png、tree-mobile.png、mascot-choice-anchor.png。
- 桌面 viewport 1440 × 900，截图 1309 × 818；手机 viewport 390 × 844。比较板按原比例缩小，旧截图仅界定改动范围；新地图底图和渲染图用于核对资产。
- 排版：沿用原字体，缩小标题；节点取代重复选择比较、时间线、时间账和独立来源折叠区。展开后左侧一张票根，右侧白底蓝边知乎卡片，移动端纵向排列。
- 修复 P2：旧 story grid 把地图挤成半栏，已提高局部选择器优先级使地图通栏；重新截图 tree-branch.png 验证。独立票根序号保留其历史索引，90 天为 02。
- 色彩及图像：中性米白纸面配浅蓝/金/绿纸雕叶片；实线和按钮使用宇宙色，未到达路段/未走分支用虚线。连线是当前数据的 canvas 图表，底图是内置 imagegen 真素材。
- 刘看山：场景角色锚定对白框上沿，不再位于选择列表之前；阅读切换选择时未向上跳。150 天 A 改为 wave，B/C 保持 stroll。截图 mascot-choice-anchor.png 验证。
- 交互：30、90 天节点展开对应已存票根；未走分支只展示当时另一选项，不补造结果；查看旧节点不改变当前位置。实际确认最终选择后，C 地图更新为第 180 天、3 次选择，新增 150 天分支。
- 手机地图可横向滚动并默认定位真实当前节点；票根和来源纵向排列。未来节点禁用。减少动态效果使用已有静帧。
- 知乎来源严格按该节点 evidenceIds 匹配；页面内缓存已检索来源，缺失时显示未保存提示，不拿当前来源冒充旧节点。
- TypeScript、生产构建和 24 条试玩路径测试通过。无待解决 P0/P1/P2。

## 自然曲线路径微调
- 主线与分支改为固定控制点的贝塞尔曲线；节点和分支高度错落分布，避免折线感。
- 重新切换宇宙后目视验证真实 canvas 渲染，节点点击仍能展开对应详情。
- 新截图：.runtime/galgame-qa/tree-curved.png。无新的 P0/P1/P2。
- final result: passed

## 场景内自定义做法与选择位置（2026-09-14）
- “我有别的做法”在当前场景的对白框内展开，复用已有草稿、提交、授权和错误处理；返回按钮恢复两个选择，不退出场景。
- 桌面选择列表下移并给左侧刘看山留出位置；手机保留全宽选择和较小位移。
- 浏览器验证校园市集第 30 天：打开自定义做法与返回选择时，纸雕背景、刘看山、知乎来源和当前进度保持一致。示例模式保留不生成自定义剧情的提示。
- TypeScript、生产构建和 action-flow 模拟提交测试通过，git diff --check 通过；未额外调用实时 AI 生成。

## 第 180 天逐幕结局（2026-09-15）
final result: passed
- 参考：用户横向三色纸雕票根截图和提供的“已检票”印章；旧结局截图界定替换范围。新增 ending-ticket-paper.png（1816×866）由 imagegen 生成，印章保留原素材，以 multiply 合成。
- 比较板：.runtime/galgame-qa/ending-comparison.jpg；截图 ending-story.png、ending-radar.png、ending-ticket.png、ending-ticket-mobile.png。桌面 viewport 1440×900，实际截图 1309×818；手机 viewport 390×844，实际截图 354×767。参考与桌面票根均保留比例；手机纵向重排为明确的响应式适配。
- 字体：沿用衬线标题和中文正文，动态故事标题取代参考图的品牌广告语。每幕至多两句结局文案，选择回顾每次一张，删除时间账。
- 布局：修复手机文本与图案重叠，改用正常文档流；修复原步骤导航遮挡结局按钮，在结局中让逐幕导航接管。重截屏后无重叠。手机可纵向滚动到操作区。
- 颜色与素材：三色立体纸雕路径、暖白纸面与真实红色印章。桌面横票、手机纵票；无用文字未嵌入图像。
- 交互：C 路线实际走完 30→90→150→180，逐幕前后导航、四节点雷达切换、最终票根浮现与落章、PNG 保存成功。全屏彩纸 canvas 矩形与 viewport 一致，7 秒停止，卸载清理动画及 resize 监听；减少动态效果时关闭。
- 数据：90 天对比 30 天，150 天对比 90 天；DOM 数字核对一致。缺失上一节点快照时不拿更早记录冒充。图表提供完整 aria-label 和可展开数值表；这些是模拟状态，不是现实能力测评。
- TypeScript、生产构建、24 条试玩路径测试通过。未调用额外实时 AI 剧情，未部署。

## 雷达背景与校园成长调整（2026-09-15）
- 使用 public/radar-a-bg.png、radar-b-bg.png、radar-c-bg.png，按宇宙选择；竖版纸雕卡保持原始比例，避免刘看山横向变形。
- 校园维度对应执行、协作、判断、成果、机会、精力；自定义故事保留原能力维度。18 个预设选项各自配置跨阶段成长，多数能力变化 6～22，部分退出/暂停选项减少机会，精力仍保留原有代价。
- 旧校园记录从初始快照重新计算各节点与票根，保留选择、故事、来源和时间预算；含自定义行动、生成路线或作品证据的记录不改写。测试覆盖幂等性、快照连续、范围限制、原选择与自定义记录隔离。
- 浏览器完整走完 A 路线，180 vs150 实际读数：执行57/49、协作43/33、判断76/58、成果47/34、机会37/44、精力56/51。背景 computedStyle 已确认为 radar-a-bg.png。
- 截图 .runtime/galgame-qa/radar-growth-background.png；类型检查、构建和24条路径测试通过。

## 现实来信重排（2026-09-15）
final result: passed
- 使用用户的旧来信截图界定问题，不复制其高文字密度。复用 public/art/return-envelope.png 与 public/zhihu-logo.svg；无需生成替代品牌素材。
- 对比板 .runtime/galgame-qa/reality-letter-comparison.jpg，桌面截图 reality-letter.png，手机 reality-letter-mobile.png。旧图为 B 来源，新预览为 A 来源；按相同“拆信后”状态比较层级、色彩、密度，来源文本不同是所选路线不同。
- 字体/内容：标题縮短为“有人也走过这段路。”；保留当幕主题、单份摘录和一个回看问题。删除整段模拟路线复述、重复引用和长说明。长摘录可展开剩余内容。
- 布局：桌面左信封右信纸，手机上下排列；米白信纸与多层纸页阴影。入口为“一封现实来信 / 拆信”，浏览器实测入口与上层容器同宽308.92px（手机）。
- 颜色/素材：知乎卡片白底、蓝色装饰边、方角、SVG Logo；已有头像和赞同数正确显示。源图信封比例保留，无拉伸。
- 交互：保留API检索、重试、空结果、原文链接、多封轮换与关闭返回；浏览器验证A保存来信打开、显示与回到故事，手机无裁切操作。未额外调用实时搜索。
- 类型检查与生产构建通过。无待解决P0/P1/P2。
