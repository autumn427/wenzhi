# 校园纸雕 Galgame 检查

final result: passed

## 设计依据与范围

用户要求改造现有故事选择区域，参考 Galgame 的场景、底部对白和场景内纸笺；最新要求明确改为三个宇宙各自的颜色和故事场景，不复刻参考山水，不放刘看山，等待后续动态元素。

- 交互构图参考：`C:/Users/Lenovo/AppData/Local/Temp/codex-clipboard-986becac-d250-4968-ba88-996514394429.png`，3184 × 1736。
- 纸雕材质参考：`C:/Users/Lenovo/AppData/Local/Temp/codex-clipboard-05e035ad-ca3f-4a86-abdc-75787341e40c.png`，860 × 573。仅用来判断切边、叠纸、镂空与层间阴影，不复刻其构图和建筑。
- 最终三个独立素材：`public/assets/galgame/parttime-blue.png`、`internship-amber.png`、`market-green.png`，均为 1672 × 941。
- 实现地址：`http://127.0.0.1:5173/?demo=1`。

## 浏览器证据

使用 Codex in-app Browser。桌面覆盖设置为 1440 × 900，当前浏览器缩放下 DOM/CSS 可视区约 1309 × 818，截图同为 1309 × 818；手机设置 390 × 844，截图约 355 × 767。引用图和实现不是相同产品或相同文案，按内容区域比例比较，不作像素复制声明；参考图显示时等比缩至 2048 × 1117。构图参考、材质参考和最终实习实现截图在同一次比较输入中打开。

- `.runtime/galgame-qa/route-a.png`：蓝色兼职店，第 30 天阅读。
- `.runtime/galgame-qa/route-b.png`：暖金实习办公区，第 30 天阅读。
- `.runtime/galgame-qa/route-c-choices.png`：绿色校园市集，第 30 天选择。
- `.runtime/galgame-qa/route-c-preview.png`：市集路线试选后续。
- `.runtime/galgame-qa/mobile-final.png`：最终手机实习阅读画面。

全景比较确认场景占主区域、对白在左下方、知乎纸笺在右侧。另逐一打开原尺寸最终素材检查纸层边缘、道具和无角色要求；手机截图检查标题折行及底部按钮。这些图中文字和卡片清晰可读，无需另外裁切细节图。

## 必检项

- 字体：沿用项目字体，章节标题缩小、正文分段，选项与按钮层级清楚。手机标题使用均衡折行，避免单独余下两字。
- 空间：场景铺满可用宽度，桌面左侧主体约三分之二，右侧卡片 280–360px；选择展开时仍保留对白操作。手机纵向排布并避让试玩退出按钮。
- 颜色：A 蓝色店铺、B 暖金办公区、C 绿色市集；继续按钮继承各宇宙深色。知乎仍为白底方角、蓝色边缘。
- 图像：三张独立内置 ImageGen 素材，裁切无拉伸，主要道具符合各自路线；无刘看山、其他角色、山水照搬或烘焙的界面文字。项目知乎 SVG 与接口头像继续使用原素材。
- 文案：原故事和选择保留，按句子分段阅读；先显示旁白，再呈现矛盾与选择。结果中的取舍收进可展开内容。知乎摘要、作者、赞同与原文链接仍可见。

## 修正与复验记录

1. P2：最初选择展开后对白按钮超出可视范围。取消外层侧边限制并改为可伸展场景区域，减少选择状态顶部占位；最终桌面选择截图中按钮完整。
2. P2：手机试玩退出控件遮住章节标题。手机顶部留出空间，同时减少无效高度；最终手机截图可读、可继续。
3. P1：旧背景偏纸模型，后续山水版不符合用户最新的三个校园主题与无角色要求。最终改为三张独立生成的蓝店铺、金办公区、绿市集；旧变体移出 public，未在页面引用。
4. P1：原试选校验仅允许 30→90，导致新增后续分岔无法继续。按当前章节严格校验 30→90→150→180，结局允许零选项，保留原历史一致性检查。浏览器已走通 C 路线至结局；自动覆盖全部 24 条路线的试选保存、恢复和禁止跳章。
5. P2：手机实习标题最后只余两字。增加手机标题均衡折行，`mobile-final.png` 记录修正后的两行标题。

## 功能与工程验证

- 浏览器实测继续、上一段、直接选择、两种结果分别试选与回看、确认进入下一幕；C 路线已完成 30→90→150→180。
- A/B/C 切换：场景、章节、配色和知乎参照同步更新。
- 正常交互控制台未发现 JavaScript 错误；素材在生成期间曾尚未就绪，最终三张均已加载并视觉确认。
- `npm run typecheck` 通过。
- `npm run build` 通过，仍有项目既有的大分块提示。
- `node scripts/test-campus-demo.mjs`：24 条校园故事路径通过。
- `node scripts/test-fork-comparison.mjs`：原有校验与新增全部章节试选/恢复检查通过。

动态刘看山为用户随后提供素材后的独立接入项，本版按最新要求不包含角色。
